// Notification: push/email/SMS for status & chat
// Maps to: FR-8.6
// SKELETON — no business logic yet. Wire routes/handlers in a later phase (docs §3.9).

export const SERVICE = 'notification' as const;

export function describe(): string {
  return 'Notification: push/email/SMS for status & chat';
}
