// Booking / Order: order lifecycle, wizard state, validation, status
// Maps to: FR-1, FR-3, FR-4
// SKELETON — no business logic yet. Wire routes/handlers in a later phase (docs §3.9).

export const SERVICE = 'booking' as const;

export function describe(): string {
  return 'Booking / Order: order lifecycle, wizard state, validation, status';
}
