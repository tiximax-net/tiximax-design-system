{
  "name": "@tiximax/express-gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "API Gateway / BFF — single entry for client apps (docs §3.2). Health-check skeleton only.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -b"
  }
}
