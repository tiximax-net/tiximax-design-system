// Tracking: milestones, stage updates, actual weight, ETA
// Maps to: FR-5
// SKELETON — no business logic yet. Wire routes/handlers in a later phase (docs §3.9).

export const SERVICE = 'tracking' as const;

export function describe(): string {
  return 'Tracking: milestones, stage updates, actual weight, ETA';
}
