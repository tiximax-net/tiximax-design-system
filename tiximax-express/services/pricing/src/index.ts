// Pricing / Quote: estimate & final price; fee tables, surcharges, FX
// Maps to: FR-2
// SKELETON — no business logic yet. Wire routes/handlers in a later phase (docs §3.9).

export const SERVICE = 'pricing' as const;

export function describe(): string {
  return 'Pricing / Quote: estimate & final price; fee tables, surcharges, FX';
}
