// Ops: backend for ops portal — confirm, update stage, enter weight
// Maps to: FR-8.7
// SKELETON — no business logic yet. Wire routes/handlers in a later phase (docs §3.9).

export const SERVICE = 'ops' as const;

export function describe(): string {
  return 'Ops: backend for ops portal — confirm, update stage, enter weight';
}
