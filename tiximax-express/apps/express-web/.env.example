# TIXIMAX Express web — environment template. Copy to `.env` and fill in.
# Base URL of the API gateway / BFF (see services/gateway). No business logic yet.
VITE_API_BASE_URL=http://localhost:4000
# WebSocket endpoint for realtime chat & tracking updates (docs §3.8).
VITE_WS_URL=ws://localhost:4000/ws
