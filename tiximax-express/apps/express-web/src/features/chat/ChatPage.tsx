import { PagePlaceholder } from '../../components/PagePlaceholder';

// Support chat — persona, quick-replies, attachments, order cards (docs FR-6).
export function ChatPage() {
  return <PagePlaceholder title="Chat" requirements="FR-6" />;
}
